scalar DateTime

input pageInput {
  current: Int!
  pageSize: Int!
}

type pageInfo {
  current: Int!
  pageSize: Int!
  total: Int!
}

enum Weekday {
  monday
  tuesday
  wednesday
  thursday
  friday
  saturday
  sunday
}

enum CardType {
  TIME
  DURATION
}

enum ProductStatus {
  UN_LIST
  LIST
}
